# 🧠 Architecture Decision Log (ADL)

В этом документе фиксируются ключевые архитектурные решения проекта, их контекст и обоснование. Это помогает понять "почему" проект построен именно так.

**Статус:** Актуален на 2025 год.

---

## 001. Выбор Next.js 16 (App Router)

* **Контекст:** Требуется современный фреймворк для создания производительного React-приложения с возможностью масштабирования.
* **Решение:** Использовать **Next.js 16** с **App Router**.
* **Обоснование:**
    * Встроенная оптимизация шрифтов (`next/font`) и изображений.
    * Поддержка React Server Components (RSC) по умолчанию, что снижает размер JS-бандла для клиента (важно для портфолио с тяжелыми анимациями).
    * Упрощенный роутинг и Layouts.

## 002. Гибридная стратегия анимаций (GSAP + Framer Motion)

* **Контекст:** Проект требует как сложных декларативных переходов (появление элементов), так и высокопроизводительных императивных анимаций (курсор, шторка загрузки).
* **Решение:** Использовать обе библиотеки, разделив зоны ответственности.
* **Обоснование:**
    * **GSAP:** Идеален для таймлайнов и анимаций, требующих `requestAnimationFrame` без лишнего оверхеда React-рендера (например, `CustomCursor` и логика `Preloader`). GSAP работает быстрее при частых обновлениях (mousemove).
    * **Framer Motion:** Идеален для React-компонентов, где анимация привязана к состоянию (`unmount`, `layoutId`) и скроллу (`useScroll`, `useTransform`). Использован в секциях `Hero` и `Projects`.
* **Последствия:** Небольшое увеличение бандла, которое оправдано качеством UX.

## 003. Стилизация через SCSS Modules

* **Контекст:** Необходимо изолировать стили компонентов, но при этом иметь доступ к возможностям препроцессора (вложенность, миксины, переменные).
* **Решение:** Использовать **SCSS Modules** (`.module.scss`).
* **Обоснование:**
    * Tailwind CSS мог бы усложнить читаемость JSX при большом количестве анимационных пропсов.
    * Модули гарантируют уникальность имен классов (нет конфликтов стилей).
    * Позволяет писать чистый CSS, который легче переносить или дебажить в DevTools.

## 004. Управление состоянием загрузки (Orchestration)

* **Контекст:** При загрузке тяжелой страницы анимации Hero-блока могут начать проигрываться до того, как исчезнет Preloader, вызывая визуальные баги ("дерганье").
* **Решение:** Паттерн "Обратного вызова" (Callback Ref pattern).
* **Реализация:**
    * `Preloader` принимает проп `onComplete`.
    * `Home` (page.tsx) хранит состояние `heroAnimationStart`.
    * Только после завершения анимации Preloader'а запускается анимация Hero.
* **Результат:** Плавный вход (Seamless Entry) без гонки состояний.

## 005. Оптимизация производительности рендеринга

* **Решение:** Принудительная акселерация GPU для тяжелых элементов.
* **Реализация:**
    * Использование `will-change: transform` для элементов скролла.
    * Использование `transform: translateZ(0)` в `Preloader`, чтобы вынести текстовые слои в отдельный композитный слой браузера.
    * Отключение кастомного курсора на устройствах с `(hover: none)` (тачскрины) для экономии ресурсов батареи.

---

## Будущие улучшения (Tech Debt & Roadmap)

1.  **Интеграция CMS:** Сейчас данные проектов захардкожены в массивах. Планируется вынос в headless CMS (Sanity или Contentful).
2.  **Типизация:** Заменить `any` в `TunnelCard` (файл `Hero/index.tsx`) на строгий интерфейс `Project`.
3.  **a11y (Доступность):** Добавить `aria-labels` для кастомных интерактивных элементов.